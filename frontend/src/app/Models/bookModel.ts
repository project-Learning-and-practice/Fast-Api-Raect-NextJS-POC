export default class book {
  title: string;
  author: string;
  studentId: string;

  constructor(title: string, author: string, studentId: string) {
    this.title = title;
    this.author = author;
    this.studentId = studentId;
  }
}
